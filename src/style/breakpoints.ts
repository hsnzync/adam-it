enum screenSizes {
    xs = 400,
    sm = 600,
    md = 1000,
    lg = 1200,
    xl = 1500,
}

export const screenMaxWidth = {
    xs: screenSizes.xs,
    sm: screenSizes.sm,
    md: screenSizes.md,
    lg: screenSizes.lg,
    xl: screenSizes.xl,
}
