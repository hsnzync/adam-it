export enum breakpoints {
    xs = 500,
    sm = 800,
    md = 900,
    lg = 1200,
    xl = 1536,
    xxl = 2000,
}

export const screenMaxWidth = {
    xs: breakpoints.xs,
    sm: breakpoints.sm,
    md: breakpoints.md,
    lg: breakpoints.lg,
    xl: breakpoints.xl,
}
